function turnIntiAMartian(myName) {

    function recallName(myName) {
        var martianName = myName + " Марсианин"
        return martianName
    }
    var martianName = recallName(myName);
    console.log(martianName);
}